import { Login as LoginForm } from '@components'
import { Fragment } from 'react'

const LoginPage = () => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  )
}

export default LoginPage
